.page-header
  %h1 Users

%table.table
  %thead
    %tr
      %th
      %th Name
      %th Email
  %tbody
    - @users.each do |user|
      %tr
        %td= image_tag avatar_url(user, 24)
        %td= link_to user.full_name, user_path(user)
        %td= user.email
