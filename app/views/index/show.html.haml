.jumbotron.text-center
  %h1 Treasure
  %p Make someone's day.

- if !current_user.nil?
  %p
    = form_for :tweet, url: tweets_path, method: :post do |f|
      = f.text_area :message, class: 'form-control'
      = f.submit "Send Tweet"
- else
  %p You must log in to send a tweet.
